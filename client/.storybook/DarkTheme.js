import { create } from '@storybook/theming/create'

export default create({
  base: 'dark',
  brandTitle: 'Cooler storybook',
  appContentBg: '#000',
  fontBase: '"Roboto", sans-serif',
  textColor: '#fff',
  textInverseColor: '#000'
})
